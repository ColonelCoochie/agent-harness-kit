# GitHub Copilot Instructions

Follow the canonical repository workflow in `AGENTS.md`. Keep credential values environment-only, preserve the zero-dependency runtime, and run `npm test` plus Node syntax checks before completion.
Inherit and enforce the canonical `## Development-Harness Boundary`, including fresh-task resume and terminal handoff rules.
